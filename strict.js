/* eslint-disable max-len, line-comment-position, no-inline-comments, sort-keys */
// Automatically generated from ESLint docs tree on April 16, 2022.

module.exports = {
    'rules': {
        'accessor-pairs': 'error', // Enforces getter/setter pairs in objects and classes.
        'array-bracket-newline': 'error', // Enforces line breaks after opening and before closing array brackets.
        'array-bracket-spacing': 'error', // Disallows or enforce spaces inside of brackets.
        'array-callback-return': 'error', // Enforces return statements in callbacks of array's methods.
        'array-element-newline': 'error', // Enforces line breaks between array elements.
        'arrow-body-style': 'error', // Requires braces in arrow function bodies.
        'arrow-parens': 'error', // Requires parens in arrow function arguments.
        'arrow-spacing': 'error', // Requires space before/after arrow function's arrow.
        'block-scoped-var': 'error', // Enforces treating `var` as block scoped.
        'block-spacing': 'error', // Disallows or enforces spaces inside of blocks after opening blocks and before closing blocks.
        'brace-style': 'error', // Enforces consistent brace style for blocks.
        'callback-return': 'error', // Enforces return after callback.
        'camelcase': 'error', // Enforces camelcase naming convention.
        'capitalized-comments': 'error', // Enforces or disallows capitalization of the first letter of a comment.
        'class-methods-use-this': 'error', // Enforces that class methods utilize `this`.
        'comma-dangle': 'error', // Requires or disallows trailing commas.
        'comma-spacing': 'error', // Enforces spacing around commas.
        'comma-style': 'error', // Enforces consistent comma styles.
        'complexity': 'error', // Enforces a maximum cyclomatic complexity.
        'computed-property-spacing': 'error', // Disallows or enforces spaces inside of computed properties.
        'consistent-return': 'error', // Requires `return` statements to either always or never specify values.
        'consistent-this': 'error', // Enforces consistent naming when capturing the current execution context.
        'constructor-super': 'error', // Verifies calls of `super()` in constructors.
        'curly': 'error', // Requires following curly brace conventions.
        'default-case-last': 'error', // Enforces default clauses in switch statements to be last.
        'default-case': 'error', // Requires a `default` case in switch statements.
        'default-param-last': 'error', // Enforces default parameters to be last.
        'dot-location': 'error', // Enforces newline before and after dots.
        'dot-notation': 'error', // Enforces dot notation whenever possible.
        'eol-last': 'error', // Requires or disallows newline at the end of files.
        'eqeqeq': 'error', // Requires the use of `===` and `!==`.
        'for-direction': 'error', // Enforces `for` loop update clause moving the counter in the right direction.
        'func-call-spacing': 'error', // Requires or disallows spacing between function identifiers and their invocations.
        'func-name-matching': 'error', // Requires function names to match the name of the variable or property to which they are assigned.
        'func-names': 'error', // Requires or disallows named `function` expressions.
        'func-style': 'error', // Enforces the consistent use of either `function` declarations or expressions.
        'function-call-argument-newline': 'error', // Enforces line breaks between arguments of a function call.
        'function-paren-newline': 'error', // Enforces consistent line breaks inside function parentheses.
        'generator-star-spacing': 'error', // Enforces spacing around the `*` in generator functions.
        'getter-return': 'error', // Enforces that a `return` statement is present in property getters.
        'global-require': 'error', // Enforces `reqfuire()` on the top-level module scope.
        'grouped-accessor-pairs': 'error', // Requires grouped accessor pairs in object literals and classes.
        'guard-for-in': 'error', // Requires `for in` loops to include an `if` statement.
        'handle-callback-err': 'error', // Enforces callback error handling.
        'id-blacklist': 'error', // Disallows specified identifiers.
        'id-denylist': 'error', // Disallows specified identifiers.
        'id-length': 'error', // Enforces minimum and maximum identifier lengths.
        'id-match': 'error', // Requires identifiers to match a specified regular expression.
        'implicit-arrow-linebreak': 'error', // Enforces the location of arrow function bodies with implicit returns.
        'indent-legacy': 'error', // Enforces consistent indentation.
        'indent': 'error', // Enforces consistent indentation.
        'init-declarations': 'error', // Requires or disallows initialization in variable declarations.
        'jsx-quotes': 'error', // Enforces the consistent use of either double or single quotes in JSX attributes.
        'key-spacing': 'error', // Enforces consistent spacing between keys and values in object literal properties.
        'keyword-spacing': 'error', // Enforces consistent spacing before and after keywords.
        'linebreak-style': 'error', // Enforces consistent linebreak style.
        'line-comment-position': 'error', // Enforces position of line comments.
        'lines-around-comment': 'error', // Requires empty lines around comments.
        'lines-around-directive': 'error', // Requires or disallow newlines around directives.
        'lines-between-class-members': 'error', // Requires or disallows an empty line between class members.
        'max-classes-per-file': 'error', // Enforces a maximum number of classes per file.
        'max-depth': 'error', // Enforces a maximum depth that blocks can be nested.
        'max-len': 'error', // Enforces a maximum line length.
        'max-lines': 'off', // Enforces a maximum file length.
        'max-lines-per-function': 'error', // Enforces a maximum function length.
        'max-nested-callbacks': 'error', // Enforces a maximum depth that callbacks can be nested.
        'max-params': 'error', // Enforces a maximum number of parameters in function definitions.
        'max-statements': 'error', // Enforces a maximum number of statements allowed in function blocks.
        'max-statements-per-line': 'error', // Enforces a maximum number of statements allowed per line.
        'multiline-comment-style': 'error', // Enforces a particular style for multiline comments.
        'multiline-ternary': 'error', // Enforces or disallows newlines between operands of ternary expressions.
        'new-cap': 'error', // Requires constructor names to begin with a capital letter.
        'newline-after-var': 'error', // Requires or disallows an empty line after variable declarations.
        'newline-before-return': 'error', // Requires an empty line before `return` statements.
        'newline-per-chained-call': 'error', // Requires a newline after each call in a method chain.
        'new-parens': 'error', // Requires parentheses when invoking a constructor with no arguments.
        'no-alert': 'error', // Disallows the use of `alert`, `confirm`, and `prompt`.
        'no-array-constructor': 'error', // Disallows `Array` constructors.
        'no-async-promise-executor': 'error', // Disallows using an async function as a Promise executor.
        'no-await-in-loop': 'error', // Disallows `await` inside of loops.
        'no-bitwise': 'error', // Disallows bitwise operators.
        'no-buffer-constructor': 'error', // Disallows use of the `Buffer()` constructor.
        'no-caller': 'error', // Disallows use of `caller`/`callee`.
        'no-case-declarations': 'error', // Disallows lexical declarations in case/default clauses.
        'no-catch-shadow': 'error', // Disallows shadowing of variables inside of catch.
        'no-class-assign': 'error', // Disallows modifying variables of class declarations.
        'no-compare-neg-zero': 'error', // Disallows comparing against `-0`.
        'no-cond-assign': 'error', // Disallows assignment operators in conditional statements.
        'no-confusing-arrow': 'error', // Disallows arrow functions where they could be confused with comparisons.
        'no-console': 'error', // Disallows the use of `console`.
        'no-constant-condition': 'error', // Disallows constant expressions in conditions.
        'no-const-assign': 'error', // Disallows modifying variables that are declared using `const`.
        'no-constructor-return': 'error', // Disallows returning values in constructor.
        'no-continue': 'error', // Disallows `continue` statements.
        'no-control-regex': 'error', // Disallows control characters in regular expressions.
        'no-debugger': 'error', // Disallows the use of `debugger`.
        'no-delete-var': 'error', // Disallows deleting variables.
        'no-div-regex': 'error', // Disallows regular expressions that look like division.
        'no-dupe-args': 'error', // Disallows duplicate arguments in `function` definitions.
        'no-dupe-class-members': 'error', // Disallows duplicate name in class members.
        'no-dupe-else-if': 'error', // Disallows duplicate conditions in `if-else-if` chains.
        'no-dupe-keys': 'error', // Disallows duplicate keys in object literals.
        'no-duplicate-case': 'error', // Disallows duplicate `case` labels.
        'no-duplicate-imports': 'error', // Disallows duplicate imports.
        'no-else-return': 'error', // Disallows `return` before `else`.
        'no-empty-character-class': 'error', // Disallows empty character classes in regular expressions.
        'no-empty-function': 'error', // Disallows empty functions.
        'no-empty': 'error', // Disallows empty block statements.
        'no-empty-pattern': 'error', // Disallows empty destructuring patterns.
        'no-eq-null': 'error', // Disallows `null` comparisons without type-checking operators,
        'no-eval': 'error', // Disallows eval().
        'no-ex-assign': 'error', // Disallows reassigning exceptions in `catch` clauses.
        'no-extend-native': 'error', // Disallows extending of native objects.
        'no-extra-bind': 'error', // Disallows unnecessary function binding.
        'no-extra-boolean-cast': 'error', // Disallows unnecessary boolean casts.
        'no-extra-label': 'error', // Disallows unnecessary labels.
        'no-extra-parens': 'error', // Disallows unnecessary parentheses.
        'no-extra-semi': 'error', // Disallows unnecessary semicolons.
        'no-fallthrough': 'error', // Disallows case statement fallthroughs.
        'no-floating-decimal': 'error', // Disallows leading or trailing decimal points in numeric literals.
        'no-func-assign': 'error', // Disallows reassigning `function` declarations.
        'no-global-assign': 'error', // Disallows assignment to native objects or read-only global variables.
        'no-implicit-coercion': 'error', // Disallows shorthand type conversions.
        'no-implicit-globals': 'error', // Disallows declarations in the global scope.
        'no-implied-eval': 'error', // Disallows the use of `eval()`-like methods.
        'no-import-assign': 'error', // Disallows assigning to imported bindings.
        'no-inline-comments': 'error', // Disallows inline comments after code.
        'no-inner-declarations': 'error', // Disallows variable or `function` declarations in nested blocks.
        'no-invalid-regexp': 'error', // Disallows invalid regular expression strings in `RegExp` constructors.
        'no-invalid-this': 'error', // Disallows use of `this` in contexts where the value of `this` is `undefined`.
        'no-irregular-whitespace': 'error', // Disallows irregular whitespace characters.
        'no-iterator': 'error', // Disallows the use of the `__iterator__` property.
        'no-labels': 'error', // Disallows labeled statements.
        'no-label-var': 'error', // Disallows labels that are variable names.
        'no-lone-blocks': 'error', // Disallows unnecessary nested blocks.
        'no-lonely-if': 'error', // Disallows `if` statements as the only statement in `else` blocks.
        'no-loop-func': 'error', // Disallows functions in loops.
        'no-loss-of-precision': 'error', // Disallows number literals that lose precision.
        'no-magic-numbers': 'error', // Disallows magic numbers.
        'no-misleading-character-class': 'error', // Disallows characters which are made with multiple code points in character class syntax.
        'no-mixed-operators': 'error', // Disallows mixes of different operators.
        'no-mixed-requires': 'error', // Disallows `require` calls to be mixed with regular variable declarations.
        'no-mixed-spaces-and-tabs': 'error', // Disallows mixed spaces and tabs for indentation.
        'no-multi-assign': 'error', // Disallows use of chained assignment expressions.
        'no-multiple-empty-lines': 'error', // Disallows multiple empty lines.
        'no-multi-spaces': 'error', // Disallows multiple consecutive spaces.
        'no-multi-str': 'error', // Disallows multiline strings.
        'no-native-reassign': 'error', // Disallows reassignment of native objects.
        'nonblock-statement-body-position': 'error', // Enforces the location of single-line statements.
        'no-negated-condition': 'error', // Disallows negated conditions.
        'no-negated-in-lhs': 'error', // Disallows negating the left operand in `in` expressions.
        'no-nested-ternary': 'error', // Disallows nested ternary expressions.
        'no-new-func': 'error', // Disallows `new` operators with the `Function` object.
        'no-new': 'error', // Disallows `new` operators outside of assignments or comparisons.
        'no-new-object': 'error', // Disallows `new` operators with the `Object` object.
        'no-new-require': 'error', // Disallows `new` operators with calls to `require`.
        'no-new-symbol': 'error', // Disallows `new` operators with the `Symbol` object.
        'no-new-wrappers': 'error', // Disallows `new` operators with the `String`, `Number`, and `Boolean` objects.
        'no-nonoctal-decimal-escape': 'error', // Disallows `\8` and `\9` escape sequences in string literals.
        'no-obj-calls': 'error', // Disallows calling global object properties as functions.
        'no-octal-escape': 'error', // Disallows octal escape sequences in string literals.
        'no-octal': 'error', // Disallows octal literals.
        'no-param-reassign': 'error', // Disallows reassignment of function parameters.
        'no-path-concat': 'error', // Disallows string concatenation when using `__dirname` and `__filename`.
        'no-plusplus': 'error', // Disallows the unary operators `++` and `--`.
        'no-process-env': 'error', // Disallows the use of `process.env`.
        'no-process-exit': 'error', // Disallows the use of `process.exit()`.
        'no-promise-executor-return': 'error', // Disallows returning values from Promise executor functions.
        'no-proto': 'error', // Disallows the use of the `__proto__` property.
        'no-prototype-builtins': 'error', // Disallows calling some `Object.prototype` methods directly on objects.
        'no-redeclare': 'error', // Disallows variable redeclarations.
        'no-regex-spaces': 'error', // Disallows multiple spaces in regular expression literals.
        'no-restricted-exports': 'error', // Disallows specified names in exports.
        'no-restricted-globals': 'error', // Disallows specific global variables.
        'no-restricted-imports': 'error', // Disallows specific imports.
        'no-restricted-modules': 'error', // Disallows Node.js modules.
        'no-restricted-properties': 'error', // Disallows certain object properties.
        'no-restricted-syntax': 'error', // Disallows specified syntax.
        'no-return-assign': 'error', // Disallows assignment operators in `return` statements.
        'no-return-await': 'error', // Disallows unnecessary `return await`.
        'no-script-url': 'error', // Disallows `javascript:` URLs.
        'no-self-assign': 'error', // Disallows assignments where both sides are exactly the same.
        'no-self-compare': 'error', // Disallows comparisons where both sides are exactly the same.
        'no-sequences': 'error', // Disallows use of the comma operator.
        'no-setter-return': 'error', // Disallows returning values from setters.
        'no-shadow': 'error', // Disallows variable declarations from shadowing variables declared in the outer scope.
        'no-shadow-restricted-names': 'error', // Disallows identifiers from shadowing restricted names.
        'no-spaced-func': 'error', // Disallows spacing between function identifiers and their applications.
        'no-sparse-arrays': 'error', // Disallows sparse arrays.
        'no-sync': 'error', // Disallows synchronous methods.
        'no-tabs': 'error', // Disallows all tabs.
        'no-template-curly-in-string': 'error', // Disallows template literal placeholder syntax in regular strings.
        'no-ternary': 'error', // Disallows ternary operators.
        'no-this-before-super': 'error', // Disallows use of `this`/`super` before calling `super()` in constructors.
        'no-throw-literal': 'error', // Restricts what can be thrown as an exception.
        'no-trailing-spaces': 'error', // Disallows trailing whitespace at the end of lines.
        'no-undefined': 'error', // Disallows the use of `undefined` as an identifier.
        'no-undef-init': 'error', // Disallows initializing variables to `undefined`.
        'no-undef': 'error', // Disallows the use of undeclared variables unless mentioned in `/*global */` comments.
        'no-underscore-dangle': 'error', // Disallows dangling underscores in identifiers.
        'no-unexpected-multiline': 'error', // Disallows confusing multiline expressions.
        'no-unmodified-loop-condition': 'error', // Disallows unmodified conditions of loops.
        'no-unneeded-ternary': 'error', // Disallows ternary operators when simpler alternatives exist.
        'no-unreachable-loop': 'error', // Disallows loops with a body that allows only one iteration.
        'no-unreachable': 'error', // Disallows unreachable code after `return`, `throw`, `continue`, and `break` statements.
        'no-unsafe-finally': 'error', // Disallows control flow statements in `finally` blocks.
        'no-unsafe-negation': 'error', // Disallows negating the left operand of relational operators.
        'no-unsafe-optional-chaining': 'error', // Disallows use of optional chaining in contexts where the `undefined` value is not allowed.
        'no-unused-expressions': 'error', // Disallows unused expressions.
        'no-unused-labels': 'error', // Disallows unused labels.
        'no-unused-vars': 'error', // Disallows unused variables.
        'no-use-before-define': 'error', // Disallows the use of variables before they are defined.
        'no-useless-backreference': 'error', // Disallows useless backreferences in regular expressions.
        'no-useless-call': 'error', // Disallows unnecessary `.call()` and `.apply()`.
        'no-useless-catch': 'error', // Disallows unnecessary catch clauses.
        'no-useless-computed-key': 'error', // Disallows unnecessary computed property keys in objects and classes.
        'no-useless-concat': 'error', // Disallows unnecessary concatenation of strings.
        'no-useless-constructor': 'error', // Disallows unnecessary constructors.
        'no-useless-escape': 'error', // Disallows unnecessary escape characters.
        'no-useless-rename': 'error', // Disallows renaming import, export, and destructured assignments to the same name.
        'no-useless-return': 'error', // Disallows redundant return statements.
        'no-var': 'error', // Requires `let` or `const` instead of `var`.
        'no-void': 'error', // Disallows use of the void operator.
        'no-warning-comments': 'error', // Disallows specified warning terms in comments.
        'no-whitespace-before-property': 'error', // Disallows whitespace before properties.
        'no-with': 'error', // Disallows `with` statements.
        'object-curly-newline': 'error', // Enforces consistent line breaks after opening and before closing braces.
        'object-curly-spacing': 'error', // Enforces consistent spacing inside braces.
        'object-property-newline': 'error', // Enforces placing object properties on separate lines.
        'object-shorthand': 'error', // Requires or disallows method and property shorthand syntax for object literals.
        'one-var-declaration-per-line': 'error', // Requires or disallows newlines around variable declarations.
        'one-var': 'error', // Enforces variables to be declared either together or separately in functions.
        'operator-assignment': 'error', // Requires or disallows assignment operator shorthand where possible.
        'operator-linebreak': 'error', // Enforces consistent linebreak style for operators.
        'padded-blocks': 'error', // Requires or disallows padding within blocks.
        'padding-line-between-statements': 'error', // Requires or disallows padding lines between statements.
        'prefer-arrow-callback': 'error', // Requires using arrow functions for callbacks.
        'prefer-const': 'error', // Requires `const` declarations for variables that are never reassigned after declared.
        'prefer-destructuring': 'error', // Requires destructuring from arrays and/or objects.
        'prefer-exponentiation-operator': 'error', // Disallows the use of `Math.pow` in favor of the `**` operator.
        'prefer-named-capture-group': 'error', // Suggest using named capture group in regular expression.
        'prefer-numeric-literals': 'error', // Disallows `parseInt()` and `Number.parseInt()` in favor of binary, octal, and hexadecimal literals.
        'prefer-object-spread': 'error', // Prefer use of an object spread over `Object.assign`.
        'prefer-promise-reject-errors': 'error', // Requires using Error objects as Promise rejection reasons.
        'prefer-reflect': 'error', // Suggest using Reflect methods where applicable.
        'prefer-regex-literals': 'error', // Disallows use of the `RegExp` constructor in favor of regular expression literals.
        'prefer-rest-params': 'error', // Suggests using rest parameters instead of `arguments`.
        'prefer-spread': 'error', // Suggests using spread syntax instead of `.apply()`.
        'prefer-template': 'error', // Suggests using template literals instead of string concatenation.
        'quote-props': 'error', // Requires quotes around object literal property names.
        'quotes': [
            'error',
            'single'
        ], // Enforces the consistent use of either backticks, double, or single quotes.
        'radix': 'error', // Enforces the consistent use of the radix argument when using `parseInt()`.
        'require-atomic-updates': 'error', // Disallows assignments that can lead to race conditions due to usage of `await` or `yield`.
        'require-await': 'error', // Disallows async functions which have no `await` expression.
        'require-jsdoc': 'error', // Requires JSDoc comments.
        'require-unicode-regexp': 'error', // Enforces the use of `u` flag on RegExp.
        'require-yield': 'error', // Disallows generator functions that do not have `yield`.
        'rest-spread-spacing': 'error', // Enforces spacing between rest and spread operators and their expressions.
        'semi': 'error', // Requires or disallows semicolons instead of ASI.
        'semi-spacing': 'error', // Enforces spacing before and after semicolons.
        'semi-style': 'error', // Enforces location of semicolons.
        'sort-imports': 'error', // Enforces sorted import declarations within modules.
        'sort-keys': 'error', // Requires object keys to be sorted.
        'sort-vars': 'error', // Requires variables within the same declaration block to be sorted.
        'space-before-blocks': 'error', // Requires Or disallows space before blocks.
        'space-before-function-paren': 'error', // Requires or disallows a space before function parenthesis.
        'spaced-comment': 'error', // Enforces consistent spacing after the `//` or `/*` in a comment.
        'space-infix-ops': 'error', // Requires spacing around infix operators.
        'space-in-parens': 'error', // Disallows or enforce spaces inside of parentheses.
        'strict': 'error', // Requires or disallow strict mode directives.
        'switch-colon-spacing': 'error', // Enforces spacing around colons of switch statements.
        'symbol-description': 'error', // Requires symbol descriptions.
        'template-curly-spacing': 'error', // Enforces usage of spacing in template strings.
        'template-tag-spacing': 'error', // Requires or disallow spacing between template tags and their literals.
        'unicode-bom': 'error', // Requires or disallows the Unicode Byte Order Mark (BOM).
        'use-isnan': 'error', // Requires calls to `isNaN()` when checking for `NaN`.
        'valid-jsdoc': 'error', // Enforces valid JSDoc comments.
        'valid-typeof': 'error', // Enforces comparing `typeof` expressions against valid strings.
        'vars-on-top': 'error', // Requires variable declarations to be at the top of their scope.
        'wrap-iife': 'error', // Requires IIFEs to be wrapped.
        'wrap-regex': 'error', // Requires regex literals to be wrapped.
        'yield-star-spacing': 'error', // Enforces spacing around the `*` in `yield*` expressions.
        'yoda': 'error' // Requires or disallows "Yoda" conditions.
    },

    'globals': {
        'module': true
    }
};
